/**
 * The construction module's typeface: Poppins, and nothing else.
 *
 * Loaded from Google Fonts in index.html (weights 400-800). `sans-serif` is only the last-resort
 * generic the browser uses for the instant before the font arrives, so no other named face
 * ever shows. Used by the contractor app and the admin construction dashboard.
 */
export const CONSTRUCTION_FONT = "'Poppins', sans-serif";
